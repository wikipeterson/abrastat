Place the real Puzzle Week packet PDF here as:

`puzzle-week-2026.pdf`

This file is intentionally not served from `/public`, so the packet can stay private until the release date.

During development, the download route falls back to a temporary placeholder PDF if the real file is missing.
